require 'rails_helper'

RSpec.describe MentorshipRequestsHelper, type: :helper do
  # MentorshipRequestsHelper is currently empty, but we include this test file
  # for completeness and future helper methods
  
  it 'exists as a module' do
    expect(MentorshipRequestsHelper).to be_a(Module)
  end
end
