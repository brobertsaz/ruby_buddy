require 'rails_helper'

RSpec.describe MessagesHelper, type: :helper do
  # MessagesHelper is currently empty, but we include this test file
  # for completeness and future helper methods
  
  it 'exists as a module' do
    expect(MessagesHelper).to be_a(Module)
  end
end
